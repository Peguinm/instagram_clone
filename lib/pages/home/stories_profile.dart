import 'package:flutter/material.dart';

class StoriesProfile extends StatelessWidget {
  const StoriesProfile({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}